# 📡 CRUD Dispositivos IoT – JavaScript Vanilla

Aplicación web para la gestión de **dispositivos IoT** que consume una **API REST** usando **JavaScript puro (Vanilla JS)**, `fetch` con `async/await` y **Bootstrap 5** para la interfaz.

Permite realizar operaciones **CRUD completas**:
- Crear
- Leer
- Actualizar
- Eliminar

---

## 🌐 API REST utilizada

